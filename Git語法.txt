未來更新程式碼的流程
第一次設定好之後，未來您只要在 VS Code 修改了任何程式碼（例如改了 CSS 顏色或是調整了 JS 邏輯），只需要依序執行這三個指令就可以更新網站了：

Bash
git add .
git commit -m "這裡寫您這次改了什麼，例如：新增自動刷新地圖功能"
git push
推上去之後，等個一兩分鐘，您的 GitHub Pages 網站就會自動更新成最新版本了！